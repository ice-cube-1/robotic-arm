async def sort():
    barrels = scan()
    colors = []
    for i in range(len(barrels)):
        colors.append(barrel[i].color,i)
    await collect("red")
    await collect("orange")
    await collect("yellow")
    await collect("green")
    await collect("blue")
    await collect("purple")

async def collect(color):
    for idx,i in enumerate(colors):
        if i[0]==color: 
            pickup(i[1])
            drop(len(colors)*25, 120)
            colours[idx][0] = "done"

await sort()